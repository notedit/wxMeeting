<!--pages/index/index.wxml-->
<view class='main'>
  <view style='width:100vw;'>
    <cameraview 
      user="user"
      pushUrl="pubshUrl"
      event="{{event}}"
      config="{{config}}"
      styles="{{styles}}"
    >
     
    </cameraview>
  </view>

</view>
