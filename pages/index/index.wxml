<!--pages/index/index.wxml-->
<view class='main'>
  <view style='width:100vw;'>
    <cameraview 
      user="{{user}}"
      event="{{event}}"
      config="{{config}}"
      styles="{{styles}}"
      pushUrl="{{pushUrl}}"
    >
    </cameraview>
  </view>

</view>
